    public PACKAGE_CLASS_NAME(CTOR_PARAMETERS) throws ThrowableTHROWABLE_TEMPLATE {
        try {
            // add reference to assemblyName.dll file
            addReference(JCOReflector.getUseFullAssemblyName() ? assemblyFullName : assemblyShortName);
            // Pass the constructor arguments directly into the initialization method
            initializeGenericArguments(getJCOClassName()CTOR_NEWOBJECT_PARAMETERS);
        } catch (JCNativeException jcne) {
            throw translateException(jcne);
        }
    }
